<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/characters/<%=characters.id%>?_method=PUT" method="POST">
    Name: <input type="text" name="name" value="<%=characters.name%>"/><br/>
    <%for (let i = 0; i < characters.characterClass.length; i++){%>
        Class: <%=i + 1 %><input type="text" name="characterClass[<%=i%>].characterClass" value="<%=characters.characterClass[i].characterClass%>"/>
        Level: <input type = "number" name = "characterClass[<%=i%>]level" value = "<%=characters.characterClass[i].level%>"/>
    <%}%>
    Is Character Dead
    <input type="checkbox" name="isDead"
        <% if (characters.isDead === true) { %>
            checked
        <% } %> 
    />
    <input type="submit" name="" value="Update Character"/>
    </form>
</body>
</html>